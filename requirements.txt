pydantic==2.11.1
python-telegram-bot[job-queue]==20.7   # <- reemplaza la línea anterior sin [job-queue]
python-binance==1.0.19                 # <- NUEVO: para modo real
ccxt==4.3.63
pandas==2.2.2
numpy==1.26.4
aiohttp==3.9.5
python-dotenv==1.0.1
PyYAML==6.0.2
pytz==2024.1
ta==0.11.0
schedule==1.2.2                        # <- NUEVO: el bot lo importa
APScheduler==3.10.4                    # <- opcional pero recomendado; lo usa el JobQueue
httpx==0.25.2                          # <- versión compatible con PTB 20.7
